# Create an ubuntu image on Raspbian

Run the script to create an image.

```
sudo ./mkimage.sh <image-name:tag> <ubuntu-version>
```

Example:

```
sudo ./mkimage.sh teemow/rpi-ubuntu:xenial xenial
```
